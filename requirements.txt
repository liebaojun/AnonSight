# AnonSight 运行依赖
#
# 只有三个，都是跨平台的大路货。后端刻意**只依赖标准库 + 这三个**，
# 就是为了让"克隆下来就能跑"这件事尽量简单。

PyMuPDF>=1.24        # PDF 解析：取文字、注释、附件（.ano 容器就是靠它的附件 API）
numpy>=1.24          # 语义布局的数值计算
scikit-learn>=1.3    # 语义布局：TF-IDF 向量化 + SVD 降维 + MDS 投影
